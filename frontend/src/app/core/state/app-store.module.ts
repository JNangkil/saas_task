import { NgModule } from '@angular/core';

/**
 * Placeholder NgRx store module. Swap with StoreModule.forRoot once the state
 * management decision (NgRx vs ComponentStore) is finalized.
 */
@NgModule({})
export class AppStoreModule {}
